#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    do
    {
    for(int i = 0; argv[i];i++)
    {
       string message = argv[i];

       for(k = 0; i );
        printf("%s\n", message);
    }
    return 0;
    }
    while(argc > 1);

}
